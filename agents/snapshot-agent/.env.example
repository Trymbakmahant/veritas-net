# Copy to .env in this folder, or export before `npm run dev`.

# HTTP port for this agent (default 8802)
PORT=8802

# Snapshot hub GraphQL endpoint (default is public hub)
SNAPSHOT_GRAPHQL=https://hub.snapshot.org/graphql
